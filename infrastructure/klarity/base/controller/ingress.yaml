apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: klarity-ingress
  namespace: monitoring
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`klarity.${DOMAIN}`)
      kind: Rule
      services:
        - name: klarity
          port: 8080
