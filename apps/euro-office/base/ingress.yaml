apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: euro-office-ingress
  namespace: nextcloud
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`office.${DOMAIN}`)
      kind: Rule
      services:
        - name: euro-office
          port: 80
