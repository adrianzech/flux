apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: actualbudget-ingress
  namespace: finance
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`finance.${DOMAIN}`)
      kind: Rule
      services:
        - name: actualbudget
          port: 5006
