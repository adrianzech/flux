---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sportarr-config-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: proxmox-csi
