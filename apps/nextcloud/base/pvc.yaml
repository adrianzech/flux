apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-main-pvc
  namespace: cloud
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: proxmox-csi
